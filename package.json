{
  "name": "ts-one",
  "version": "1.0.0",
  "private": true,
  "description": "TS-One (Tribal Scholar One) — Unified Scholarship Platform for Scheduled Tribe Students, SIH 2026 (Ministry of Tribal Affairs)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "npm --prefix server start",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
